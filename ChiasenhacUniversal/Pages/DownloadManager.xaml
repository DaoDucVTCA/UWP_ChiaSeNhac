<Page
    x:Class="ChiasenhacUniversal.Pages.DownloadManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChiasenhacUniversal.Pages"
    xmlns:ctl="using:ChiasenhacUniversal.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="Black" Opacity="0.5"></SolidColorBrush>
        </Grid.Background>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <ctl:PageHeader Grid.ColumnSpan="2" VerticalAlignment="Top">
            <ctl:PageHeader.HeaderContent>
                <TextBlock Text="Quản lý download" FontWeight="SemiBold" FontSize="20" VerticalAlignment="Top" Style="{ThemeResource PageTitleTextBlockStyle}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="10,0,12,4"/>
            </ctl:PageHeader.HeaderContent>
        </ctl:PageHeader>

        <Grid Grid.Row="1" Loaded="Grid_Loaded">
            <ListView Name="lvDownload">

            </ListView>
        </Grid>
    </Grid>
</Page>
